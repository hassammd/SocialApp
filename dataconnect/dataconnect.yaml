specVersion: "v1"
serviceId: "socialapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "socialapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
